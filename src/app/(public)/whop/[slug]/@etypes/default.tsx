// src/app/(public)/whop/[slug]/@etypes/default.tsx
// Fallback slot for parallel route. Render nothing when no @etypes segment is provided.

export default function DefaultEtypesSlot() {
  return null;
}
